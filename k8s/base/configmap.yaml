apiVersion: v1
kind: ConfigMap
metadata:
  name: ncism-config
  namespace: ncism
data:
  NODE_ENV: "production"
  PORT: "5000"
  DATABASE_URL: "" # Will be set via secret
  JWT_SECRET: "" # Will be set via secret
  GEMINI_API_KEY: "" # Will be set via secret
  REDIS_URL: "redis://redis-service:6379"
  UPLOAD_DIR: "/app/uploads"
  MAX_FILE_SIZE: "10485760"

